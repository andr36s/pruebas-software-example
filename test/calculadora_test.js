const test = require('node:test');
const assert = require('node:assert');
const { calcularAreaCirculo } = require('../calculators/calcular-area-circulo');

test('test_areaCirculo', () => {
    assert.strictEqual(calcularAreaCirculo(10), 10)
})

test('test_areaCirculo', () => {
    assert.strictEqual(calcularAreaCirculo(10), 10)
})

test('test_areaCirculo', () => {
    assert.strictEqual(calcularAreaCirculo(10), 10)
})

test('test_areaCirculo', () => {
    assert.strictEqual(calcularAreaCirculo(10), 10)
})

test('test_areaCirculo', () => {
    assert.strictEqual(calcularAreaCirculo(10), 10)
})

test('test_areaCirculo', () => {
    assert.strictEqual(calcularAreaCirculo(10), 10)
})
